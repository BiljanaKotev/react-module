function Navbar() {
  return (
    <div>
      <ul>
        <li> HomePage</li>
        <li>Abut us</li>
        <li>Contact</li>
      </ul>
    </div>
  );
}

// 1. Name of component should always start with a capital letter
// 2. all elements should be nested in 1 parent element
// 3. all components need a return statement

// exports my component so I'm able to use it anywhere in my application
export default Navbar;
